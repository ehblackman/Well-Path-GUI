[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Well-Path-GUI"
version = "0.0.1"

description = "Well-Path-GUI"
readme = "README.md"
requires-python = ">=3.7"
